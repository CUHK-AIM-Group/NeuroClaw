# T328_mlflow_tracking_server: MLflow Tracking Server Setup
## Task Description

Deploy a local MLflow tracking server (sqlite backend + artifact store), point the training scripts at it, and log a sample run.

## Input Requirement


- No interactive input.


## Constraints

- Server start script + env config.

- Sample run visible in UI (screenshot or export).

- Save all generated artifacts to:
  - benchmark_results/T328_mlflow_tracking_server/


## Expected Output

Expected output artifact(s):

- `mlflow_server.sh`

- `sample_run_export.json`


Recommended metadata file:

- result_YYYYMMDD_HHMMSS.json


## Evaluation

- This test case is manually evaluated.
