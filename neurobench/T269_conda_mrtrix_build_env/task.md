# T269_conda_mrtrix_build_env: Conda Env: MRtrix3 Build Environment
## Task Description

Create an environment capable of building MRtrix3 from source (compiler toolchain, Eigen, Qt) and document the build.

## Input Requirement


- No interactive input.


## Constraints

- Document compiler versions.

- If build is skipped, provide the binary-install alternative and say why.

- Save all generated artifacts to:
  - benchmark_results/T269_conda_mrtrix_build_env/


## Expected Output

Expected output artifact(s):

- `environment.yml`

- `build_log.txt`


Recommended metadata file:

- result_YYYYMMDD_HHMMSS.json


## Evaluation

- This test case is manually evaluated.
